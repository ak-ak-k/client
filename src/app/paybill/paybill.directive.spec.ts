import { PaybillDirective } from './paybill.directive';

describe('PaybillDirective', () => {
  it('should create an instance', () => {
    const directive = new PaybillDirective();
    expect(directive).toBeTruthy();
  });
});
